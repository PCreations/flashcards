{
  "functions": {
    "source": "src",
    "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run test:unit"]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
