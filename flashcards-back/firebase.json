{
  "functions": {
    "source": "src",
    "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run test"]
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
